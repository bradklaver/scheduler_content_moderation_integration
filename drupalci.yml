build:
  assessment:
    validate_codebase:
      phplint: {  }
      container_composer:
        options: ' install --prefer-dist --no-suggest --no-progress --no-interaction'
        halt-on-fail: true
      csslint:
        halt-on-fail: false
      eslint:
        halt-on-fail: false
      phpcs:
        sniff-all-files: false
        halt-on-fail: false
        coder-version: ^8.2@stable
      container_command:
        commands:
          # Get latest dev version of Scheduler
          - "cd ${SOURCE_DIR} && sudo -u www-data composer require drupal/scheduler:1.x-dev"
          # Apply the Scheduler non-node entity patch
          - 'cd ${SOURCE_DIR}/modules/contrib/scheduler; pwd; sudo -u www-data curl "https://git.drupalcode.org/project/scheduler/-/merge_requests/3.diff" | sudo -u www-data patch -p1'
    testing:
      run_tests.standard:
        types: 'Simpletest,PHPUnit-Unit,PHPUnit-Kernel,PHPUnit-Functional'
        testgroups: '--all'
        color: true
        die-on-fail: false
        keep-results: true
        keep-results-table: false
        verbose: false
        concurrency: 0
        halt-on-fail: false
        repeat: 1
        suppress-deprecations: true
